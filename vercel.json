{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npx prisma generate && next build",
  "outputDirectory": ".next",
  "regions": ["hnd1"],
  "crons": [],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . 2>/dev/null || false"
}
